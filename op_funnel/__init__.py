# -*- coding: utf-8 -*-
"""Раздел «Воронка ОП» — ежедневная воронка обзвона по направлениям отдела продаж.

Четыре супервайзера отдела продаж ведут одну и ту же отчётность в Excel, каждый
свою: задачи #301 (Яндекс Регистрация / Поток-2), #302 (Основа ОП), #303 (Поток),
#305 (Верификатор). Раздел переносит эти файлы в портал: те же показатели, те же
формулы, но данные подтягиваются сами, а к срезу «на сегодня» добавляется
динамика по дням, сравнение периодов и разбор причин с выходом на конкретных
людей.

Направление = код модели расчёта, а не `direction_id`
-----------------------------------------------------
Таб опирается на `calculation_model_code` (op_osnova / op_potok / op_yandex_reg /
op_verificator). Причина практическая: переименование направления в портале
обнуляет `direction_id` у операторов, а код модели живёт и на группе, и у
направления. Группа 38 «Поток 2» и группа 14 «Поток» — это одно направление с
двумя базами (`stream_type` 1 и 2), и по коду модели они сходятся сами.

Что важно знать про предметную область (проверено на данных 11.09.2026)
-----------------------------------------------------------------------

* **СРМ переписывает прошлое.** Снимок супервайзера за 01.09 против текущего
  ответа партнёрской ручки: состав лидов совпал ровно (1125 против 1125), а
  исходы уехали — Дозвон 499 → 534, Согласия 257 → 267. У одного оператора все
  108 лидов ушли в «свободные». Поэтому суточный итог ФИКСИРУЕТСЯ, а расхождения
  после пересчёта складываются в журнал, а не прячутся.

* **Формулы воспроизведены, а не сочинены.** Разбор «Потока» сверен с файлом
  супервайзера построчно: 140 строк «оператор × сутки» за 01–07.09, ноль
  расхождений по «Исходу», «Дозвону» и «Согласиям». У «Основы» совпали согласия
  и нецелевые; отказы упираются в одно решение супервайзера — куда относить
  «технические» причины amoCRM («Диалоги», «YaPROREG», «Дожим приглашенные»,
  закрытые без причины). По умолчанию они идут в отдельную группу «увели», и
  справочник причин это позволяет переложить без релиза.

* **Имена операторов в источниках и в портале не совпадают** («Кузембаева» против
  «Кузембекова», «Жұмаханбет» против «Жуманхабет», «Сарсенбаева Эльдана» против
  «Елдана Сарсенбаева»), а amoCRM вместо имени отдаёт числовой id, потому что
  `/api/v4/users` закрыт (403 «Admin access only»). Поэтому соответствие
  подтверждает человек, а похожесть работает только подсказкой.

* **У Верификаторов нет часов.** Часы в портале считаются из статусов iCORE
  Phone, а верификаторы работают в чатах: за 01–11.09 у группы 13 записи в
  `daily_hours` есть, а `work_time` во всех нулевой. Решение владельца — брать
  часы по графику смен, и в интерфейсе это помечено.

Состав пакета (разделение то же, что у cdr/ и parcels/)
-------------------------------------------------------
    schema.py      DDL таблиц op_funnel_*, идемпотентный, + сид норм
    metrics.py     вся математика воронки; ни базы, ни сети
    access.py      чистая логика прав
    queries.py     SQL поверх готового курсора
    crm_client.py  клиент партнёрских ручек СРМ (stream-leads, paid-hire-leads)
    sources.py     приведение сырья источников к строкам лидов + агрегат чатов
    sync.py        выкачка суток и фиксация итогов
    report.py      книга xlsx
    routes.py      Blueprint /api/op_funnel
"""

__all__ = ['access', 'crm_client', 'metrics', 'queries', 'report', 'schema',
           'sources', 'sync']
