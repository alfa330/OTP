# -*- coding: utf-8 -*-
"""«Табло ОП» — онлайн-мониторинг отдела продаж на телефонии FreePBX.

Аналог «Табло СЗоВ» (направление «Линия»), но источники другие, и это определяет
устройство раздела целиком:

  * цифры дня — из касаний (`cdr_touches`), которые мост внутри корпоративной сети
    досылает живым хвостом каждые двадцать секунд (`cdr_bridge/live.py`). Портал к
    станции не ходит никогда — это правило раздела «Касания», и табло его не отменяет;
  * люди и их состояние — из событий iCORE Phone (`operator_status_events`), тот же
    источник и тот же каталог статусов, что у списка людей на табло Тез КЦ;
  * очереди «сейчас» (сколько ждут в эту секунду) здесь НЕТ: CDR знает о звонке
    только после его завершения, а живые ручки станции роняли её в августе 2026.
    Появится, когда станция отдаст `queue_log` или учётную запись AMI только на чтение.

Модуль чистый там, где это возможно: `snapshot.py` считает всё по спискам словарей и
проверяется без базы; `routes.py` получает зависимости аргументами фабрики, как
`cdr/routes.py`, — обратный импорт `bot_schedule2` был бы циклом.
"""
