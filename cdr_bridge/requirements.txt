requests>=2.31.0
# Подпись запросов к порталу (Ed25519). Общий токен остаётся только как
# запасной путь на время переезда — см. cdr_bridge/signing.py.
cryptography>=45.0.0
# Журнал очередей станции (cdr_bridge/pbxdb.py) — чистый Python, без сборки.
# Без пакета мост работает как раньше, только без точных ожиданий.
PyMySQL>=1.2.0
