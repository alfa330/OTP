# -*- coding: utf-8 -*-
"""Мост «Касания» — половина раздела, которая живёт внутри корпоративной сети.

Портал (Render) до станции не дотягивается и дотягиваться не должен: внешний
доступ к CDR открывали 25.08.2026 и в тот же день закрыли, потому что сервис лёг.
Поэтому за данными ходит этот мост, а к порталу он обращается сам — только
исходящими запросами, без единого открытого порта.

    station.py  клиент CDR станции с белым списком ручек (AMI не трогаем)
    agent.py    цикл: спросить задание → прочитать сутки → склеить → отправить

Склейка строк в касания берётся из `cdr.touches` — того же модуля, что на
портале. Второй копии этой логики быть не должно.

Установка и эксплуатация — README.md рядом.
"""

__all__ = ['station', 'agent']
