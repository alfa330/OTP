# -*- coding: utf-8 -*-
"""Клиент amoCRM.

Каталог назван `amocrm`, а не `amo`, намеренно: короткое имя `amo` уже занято
локальными выгрузками в outputs/ (`from amo import Amo`), и пакет в корне
репозитория начал бы их затенять.

Состав пакета
-------------
    leads.py   лиды по источникам: выгрузка сделок и отбивка в Telegram

Клиент общий, а не часть раздела: им пользуются и olx_amo/, и op_funnel/.

Импортировать с сохранением локального имени
--------------------------------------------
    from amocrm import leads as amo_leads

Так все обращения вида `amo_leads.fetch_*` в коде остаются как были — их больше
сотни, и переписывать их ради переезда каталога не нужно.

Формулы из amoCRM НЕ читаются
-----------------------------
Партнёрская ручка отдаёт готовые значения, а не выражения, и /api/v4/users закрыт
(403 «Admin access only») — вместо имени приходит числовой id. Поэтому имена
операторов подтверждает человек, а похожесть работает только подсказкой.
"""

__all__ = ['leads']
