# -*- coding: utf-8 -*-
"""Генерация текстов через ИИ: обратная связь, поздравления, заявки в IT.

Состав пакета
-------------
    service.py   три точки входа поверх Gemini:
                 generate_monthly_feedback_with_ai  — месячная ОС оператору
                 generate_birthday_greeting_with_ai — поздравление ко дню рождения
                 generate_it_ticket_with_ai         — черновик заявки в IT

Заодно переездом исправлена опечатка в имени: было ai_feed_back_service.

Импорт в bot_schedule2 отложенный — ПО ПРИЧИНЕ
----------------------------------------------
На уровне модуля service.py делает `from database import db`, то есть поднимает
пул к боевой базе прямо на импорте. Поэтому бот импортирует его внутри функции, а
не в шапке, и __init__.py здесь ничего не подгружает. Тест, которому нужен модуль
с заглушкой вместо базы, обязан сбросить И sys.modules['ai_feedback.service'], И
атрибут `service` у пакета — иначе модуль вернётся из кэша невыполненным
(см. tests/test_it_ticket_ai_request.py).
"""

__all__ = ['service']
