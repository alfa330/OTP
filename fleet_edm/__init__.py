"""Раздел «Провайдер ЭДО» — подстановка провайдера электронного документооборота
водителям по списку из диспетчерской Яндекс.Fleet (задача #176).

Человек загружает выгрузку из кабинета, робот проходит по списку и возвращает тот
же файл с колонкой «Провайдер ЭДО». Раньше это делалось разово руками
разработчика: обход 86 парков занимал часы, а сделать его сам заказчик не мог.

Устройство пакета:

    access.py   права раздела (чистая логика, без Flask и базы)
    client.py   HTTP-клиент кабинета Fleet: куки, троттлинг, протухшая сессия
    engine.py   сам обход: парки → пачки → провайдеры → добор карточками → сверка
    report.py   сборка xlsx
    queries.py  задания и сессия в базе
    routes.py   Blueprint /api/fleet_edm
    schema.py   таблицы fleet_edm_*

Почему провайдер добывается так странно (пересечением фильтров, а не чтением
поля) — см. подробный разбор в client.py: в списке контрагентов поля ЭДО нет
вообще, оно есть только в карточке водителя, а карточка стоит один запрос на
человека.
"""
