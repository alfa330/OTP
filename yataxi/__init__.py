# -*- coding: utf-8 -*-
"""Клиенты партнёрской CRM backend.yataxi.kz.

Оба модуля ходят в одну и ту же внешнюю систему и держат один и тот же доступ:
`front_office_calls` берёт конфигурацию у `reg_contest.get_config()`, а не заводит
свою. Поэтому они в одном каталоге, названном по внешней системе — как olx_amo/
и fleet_edm/.

Состав пакета
-------------
    reg_contest.py         топ по регистрациям: выгрузка, сопоставление операторов
                           с директорией портала, защита от усыхания снимка
    front_office_calls.py  статистика звонков фронт-офисов по регионам
                           (POST /api/partners/region-call-stats)

СРМ переписывает прошлое
------------------------
Ответ по уже закрытым суткам меняется задним числом: состав строк совпадает, а
исходы уезжают. Отсюда `check_snapshot_shrink` в reg_contest — суточный итог
фиксируется, а расхождения складываются в журнал, а не прячутся.

Конфигурация читается из .env.codex.local ОТ ТЕКУЩЕГО каталога запуска
(reg_contest.get_config), а не от каталога модуля, — переезд в пакет на это не
влияет.
"""

__all__ = ['front_office_calls', 'reg_contest']
