/* Модель телефона, снятая под углом, — общий ресурс портала.
 *
 * Лежит здесь, а не в папке тренажёров, чтобы её можно было взять в любой
 * другой раздел: это просто картинка устройства с ПУСТЫМ БЕЛЫМ экраном, на
 * который что угодно кладётся сверху.
 *
 * Откуда взялась: рендер владельца. Фон вокруг корпуса вырезан заливкой от
 * краёв кадра — «все белые пиксели» вырезать нельзя, внутри белым нарисованы
 * блики на корпусе. Чёрный экран перекрашен в белый по точному значению
 * (у экрана 0,0,0, у плиток приложений — 38,38,38, поэтому плитки не пострадали).
 *
 * Рядом лежат готовые варианты со значками приложений: phone-taxi-pro.png и
 * phone-sapar.png. Значок там ЧАСТЬ рендера, а не наложен сверху: в рендере он
 * уже стоит под нужным углом и в нужном месте, и накладывать поверх второй —
 * значит подбирать наклон заново и получать кривую плитку.
 *
 * SCREEN — геометрия экрана В ДОЛЯХ картинки, на случай если понадобится
 * положить на пустой экран что-то своё (скриншот, другой значок).
 */

import blank from './phone-tilted.png';
import taxiPro from './phone-taxi-pro.png';
import sapar from './phone-sapar.png';

export const PHONE_TILTED = {
    /* Пустой белый экран. */
    src: blank,
    /* Пропорции кадра: 320 × 395. */
    aspect: 320 / 395,

    /* Готовые модели со значками приложений на экране. */
    withApp: {
        'taxi-pro': taxiPro,
        sapar,
    },

    SCREEN: {
        /* Центр экрана в долях ширины и высоты картинки. */
        center: { x: 0.486, y: 0.482 },
        /* Ширина экрана в долях ширины картинки. */
        width: 0.514,
        /* Наклон длинной оси корпуса, градусы (против часовой). */
        rotate: -21.9,
        /* Углы экрана в долях картинки — если понадобится точная плоскость. */
        corners: {
            topLeft: { x: 0.4375, y: 0.0405 },
            topRight: { x: 0.9500, y: 0.0658 },
            bottomRight: { x: 0.5375, y: 0.9342 },
            bottomLeft: { x: 0.0187, y: 0.8861 },
        },
    },
};

export default PHONE_TILTED;
