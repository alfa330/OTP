/* Модель телефона, снятая под углом, — общий ресурс портала.
 *
 * Лежит здесь, а не в папке тренажёров, чтобы её можно было взять в любой
 * другой раздел: это просто картинка устройства с ПУСТЫМ БЕЛЫМ экраном, на
 * который что угодно кладётся сверху.
 *
 * Откуда взялась: рендер, который дал владелец (`Requirements` из его загрузок).
 * Фон вырезан заливкой от краёв кадра — «все белые пиксели» вырезать нельзя,
 * внутри белым нарисованы блики на корпусе. Экран перекрашен в белый по
 * пикселям обоев внутри четырёхугольника экрана: у экрана скруглённые углы, и
 * заливка прямоугольником оставила бы по ним серпики старой картинки.
 *
 * SCREEN — геометрия экрана В ДОЛЯХ картинки. Она нужна каждому, кто захочет
 * положить на экран значок или скриншот: без этих чисел придётся заново
 * подбирать положение и наклон на глаз.
 */

import src from './phone-tilted.png';

export const PHONE_TILTED = {
    src,
    /* Пропорции кадра: 440 × 542. */
    aspect: 440 / 542,

    SCREEN: {
        /* Центр экрана в долях ширины и высоты картинки. */
        center: { x: 0.499, y: 0.495 },
        /* Ширина экрана в долях ширины картинки (219 из 440). */
        width: 0.498,
        /* Наклон корпуса: длинная ось экрана отклонена на 20,5° против часовой. */
        rotate: -20.5,
        /* Ракурс: по горизонтали экран сжат сильнее, чем по вертикали. */
        scaleX: 0.9,
        /* Углы экрана в долях картинки — если понадобится точная плоскость. */
        corners: {
            topLeft: { x: 0.4585, y: 0.0171 },
            topRight: { x: 0.9532, y: 0.0588 },
            bottomRight: { x: 0.5400, y: 0.9841 },
            bottomLeft: { x: 0.0422, y: 0.9204 },
        },
    },
};

export default PHONE_TILTED;
