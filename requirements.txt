aiogram==2.25.2
flask==3.0.2
apscheduler==3.10.4
requests==2.31.0
beautifulsoup4==4.12.3
python-dotenv==1.0.1
openpyxl==3.1.5
xlsxwriter==3.2.3
flask-cors==4.0.1
psycopg2-binary==2.9.9
passlib==1.7.4
google-cloud-storage==2.18.2
pandas==2.2.2
ortools==9.15.6755
packaging>=23.0
loguru==0.7.3
httpx==0.28.1
PyJWT==2.10.1
Pillow==10.4.0
xhtml2pdf==0.2.17
weasyprint==62.3
waitress==3.0.0
# Раздел «Вики»: серверная санитизация HTML статей. Клиентского DOMPurify
# недостаточно — он защищает того, кто отправляет, а не того, кто читает.
nh3==0.3.6
# Раздел «Вики», импорт документов: mammoth — тот же конвертер DOCX,
# что в оригинале (тот же автор, та же карта стилей); pypdf вместо
# pdf-parse. XLSX/CSV разбираем уже имеющимся openpyxl.
mammoth==1.12.0
pypdf==6.15.0
# call_qa (ИИ-оценка звонков): Claude и Soniox вызываются по HTTP через httpx/requests,
# отдельные SDK не нужны. google-cloud-aiplatform (эмбеддинги/RAG) добавим вместе с pgvector.
