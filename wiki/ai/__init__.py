# -*- coding: utf-8 -*-
"""ИИ-помощник раздела «Вики»: нарезка статей, индекс, поиск и ответ.

Про связь с call_qa. Хеш текста считается здесь своим (три строки на hashlib), а
вот провайдер эмбеддингов ПЕРЕИСПОЛЬЗУЕТСЯ из call_qa/embeddings/provider.py:
там уже сделано главное — раздельные роли query и document (у многоязычной модели
Vertex это разные task_type, и путать их значит молча терять качество) и проверка
размерности на каждой границе. Дублировать это значило бы держать две копии
логики в синхроне, что хуже импорта: все __init__.py пакета call_qa пусты, а его
config при импорте только читает окружение.

Импорт при этом ленивый, внутри функции (wiki/ai/embed.py): раздел «Вики»
обязан подниматься, даже если провайдер сломан или ключа нет — тогда помощник
работает на одной лексике, а не падает весь раздел.
"""
