# -*- coding: utf-8 -*-
"""Ответ помощника: промпт, гейт уточнения, источники и защита от выдумки.

ЦИТАТУ ИЗВЛЕКАЕТ СЕРВЕР, а модель указывает только номера фрагментов. Так стало
после проверки на проде: требование «процитируй дословно» оказалось ненадёжным
механизмом и, что хуже, отбрасывало ВЕРНЫЕ ответы. На вопросе «Офис Астана» поиск
нашёл нужный кусок (близость 0,834, в тексте «Город: Астана; Адрес: Проспект
Сарыарка, 31»), ответ существовал — но модель процитировала строку-метку
«Статья «Адреса офисов», раздел «…»», сверка не нашла её в тексте, и защита
выдала отказ. На соседнем вопросе та же модель цитировала корректно, то есть
механизм срабатывал через раз. Ненадёжная защита хуже отсутствующей: помощник,
отрицающий то, что знает, теряет доверие целиком.

От выдумки теперь защищает ДРУГАЯ проверка, машинная и устойчивая: числа из
ответа обязаны встречаться в переданных фрагментах. Именно в числах живёт риск —
сумма, срок, номер телефона, адрес; а пересказ смысла своими словами перестал
быть поводом придержать ответ.

Три решения ниже опираются на замеры, а не на общие соображения.

1. ПРАВИЛО ЯЗЫКА обязано явно разрешать перевод контекста. Формулировка «отвечай
   на языке вопроса» слишком слаба: все фрагменты статей русские, и модель
   тянется за языком контекста — на казахский вопрос отвечала по-русски. С
   добавлением «фрагменты могут быть на русском: переводи их содержание» все
   проверенные модели отвечают беглым казахским.

2. ОТКАЗ ДЕЛАЕТ ПОРОГ, А НЕ ПРОМПТ. На вопросе «сколько мне отпускных положено»
   лучшая близость 0,661: куски в контекст не попадают вовсе, и модель отвечает
   отказом просто потому, что ей нечего пересказывать. Это надёжнее любых
   уговоров, поэтому STRICT_FLOOR — часть механики ответа, а не настройка поиска.

3. УТОЧНЯЮЩИЙ ВОПРОС ПРОМПТОМ НЕ ДОСТИГАЕТСЯ. Замер: на заведомо мутное «что с
   машиной» обе проверенные модели вместо вопроса выдали ответ про мойку кузова,
   зацепившись за кусок с близостью 0,725 — правило №4 в промпте не сработало ни
   у одной. Поэтому неоднозначность определяется КОДОМ (should_clarify) до вызова
   модели, а промпт лишь дублирует правило.
"""

import re

# Порог, ниже которого кусок в контекст не кладём. Измерен: см. пункт 2 шапки.
STRICT_FLOOR = 0.72

# Неоднозначность: короткий запрос + невысокая лучшая близость + разброс попаданий
# по несвязанным статьям. Числа с замера «что с машиной»: 3 слова, 0,725, 2 статьи.
CLARIFY_MAX_WORDS = 3
CLARIFY_SIMILARITY = 0.78
CLARIFY_MIN_ARTICLES = 2

# Порог, ниже которого кусок в источники не идёт. Совпавшее число весит 5, общее
# слово — 1, порог 4: то есть нужен либо один совпавший факт-число, либо четыре
# общих слова. Порог подобран по мусору, который лез при более мягких правилах: к
# вопросу о сроке аренды подшивалось «Eazy Go — сервис аренды электровелосипедов»
# (одно слово «аренды»), а к длинному ответу про байгу — статья про грузчиков и
# «Баллы приоритета» (три частых слова). Источник, ничего не подтверждающий, хуже
# отсутствующего: он выглядит доказательством, не будучи им.
SUPPORT_FLOOR = 4.0

_WORD = re.compile(r'[^\W\d_]{3,}', re.UNICODE)
# Маркер ищется ГДЕ УГОДНО, а не только с начала строки. Замер: gemini-3-flash
# закончил отказ фразой «…обращаться к руководителю отдела. ИСТОЧНИКИ: [1]» — на
# одной строке с текстом. Привязка к началу строки такое не ловила, и служебный
# маркер уезжал оператору в ответ.
_SOURCES_HEADER = re.compile(r'\s*ИСТОЧНИКИ\s*:?', re.I)
_FRAGMENT_REF = re.compile(r'\[(\d+)\]')
# Числа от двух знаков: одиночные цифры — это почти всегда нумерация списка.
_NUMBER = re.compile(r'\d[\d\s.,:/-]*\d')

SYSTEM_PROMPT = """Ты — справочный помощник корпоративной вики таксопарка. Отвечаешь операторам колл-центра.

ТВОЯ ЗАДАЧА — ПОМОЧЬ. Оператор разговаривает с водителем и ждёт ответа сейчас.
Отказ вместо ответа, который есть во фрагментах, — худшая из ошибок: он стоит
доверия ко всему помощнику. Вторая по тяжести — выдуманный ответ. Между ними и
работай: выжимай из фрагментов всё, что относится к вопросу, и не добавляй
ничего сверх них.

ЖЁСТКИЕ ПРАВИЛА
1. Отвечай ТОЛЬКО по приведённым фрагментам статей. Ничего не додумывай и не
   добавляй из общих знаний.
2. ПРОЧИТАЙ ФРАГМЕНТЫ ЦЕЛИКОМ, прежде чем сказать, что ответа нет. Нужное часто
   лежит строкой таблицы или пунктом в середине, а не заголовком.
3. НАЗВАНИЕ В ВОПРОСЕ МОЖЕТ ОТЛИЧАТЬСЯ ОТ НАЗВАНИЯ В СТАТЬЕ. «Жетіқазына»,
   «Жеті қазына», «Семь казына» и «7 Қазына» — одно и то же: числительное
   словом или цифрой, казахское написание или русское. Отличаться может и одна
   буква: вопрос приходит из распознавания речи, а оно путает гласные в
   незнакомых ему названиях. Если во фрагментах есть явно та же вещь под другим
   написанием — отвечай про неё, назвав написание из статьи. Не отказывай из-за
   расхождения в написании.
4. ЕСЛИ ТОЧНОГО ОТВЕТА НЕТ, А БЛИЗКОЕ ЕСТЬ — скажи, чего именно нет, и сразу
   дай то, что есть: соседний факт, смежное правило, похожее название. Не
   выдавай смежный факт за ответ на вопрос, но и не молчи о нём.
   Фраза «В доступных вам статьях этого нет» уместна ТОЛЬКО когда во фрагментах
   нет ничего по теме вовсе; тогда назови, к кому обратиться.
   ФРАГМЕНТЫ — ВЫДЕРЖКИ, А НЕ СТАТЬЯ ЦЕЛИКОМ. Поэтому не рассказывай, чего в
   статье нет, и не перечисляй, что в ней «только» есть: у статьи со списком
   тебе показали три строки из тридцати. Говори про то, что видишь, а про
   остальное — «в показанных мне фрагментах этого нет».
5. Коротко и по делу: без вступлений, без пересказа вопроса, без воды и без
   выводов в конце. Сразу суть. Списком, если пунктов несколько. Если данные
   табличные — город, цена, срок, парк, комиссия — оформи ТАБЛИЦЕЙ Markdown,
   она читается быстрее списка. Разметка (списки, таблицы, выделение) в
   интерфейсе отображается, поэтому пользуйся ею там, где она помогает.
6. Если вопрос неоднозначен и от уточнения зависит ответ — задай ОДИН уточняющий
   вопрос вместо ответа.
7. ЯЗЫК ОТВЕТА обязан совпадать с языком вопроса. Вопрос на казахском — весь
   ответ на казахском, включая пункты списка. Фрагменты статей могут быть на
   русском: переводи их содержание, но не переходи на русский язык в ответе.
   Если язык вопроса определить нельзя (вопрос из одного-двух слов, слова
   пишутся одинаково в обоих языках) — отвечай ПО-РУССКИ.
8. Числа, суммы, сроки, названия и ССЫЛКИ переноси ДОСЛОВНО из фрагментов. Не
   округляй и не пересчитывай. Ссылка во фрагменте выглядит как «ярлык (адрес)» —
   если спрашивают, куда идти или где зарегистрироваться, дай сам адрес, а не
   пересказ «есть ссылка в статье».

ФОРМАТ ОТВЕТА
Сначала сам ответ. Затем с новой строки ровно так:
ИСТОЧНИКИ: [номера фрагментов, из которых взяты факты]
Например: ИСТОЧНИКИ: [1] [4]

Только номера — цитату подберёт программа сама. Указывай лишь те фрагменты,
факты из которых действительно попали в ответ.
Если ответа нет или ты задаёшь уточняющий вопрос — строку ИСТОЧНИКИ не пиши."""


_KZ_LETTERS = set('әғқңөұүһі')
_KZ_MARKERS = ('керек', 'қалай', 'қанша', 'қандай', 'болса', 'үшін', 'мен ',
               'бар ', 'жоқ', 'ма?', 'ме?', 'не істеу')


_ANY_WORD = re.compile(r'[^\W\d_]+', re.UNICODE)


def _language_of(text):
    """Язык одной строки по её собственным признакам.

    Казахские буквы считаются ПО СЛОВАМ, а не по всей строке. Раньше хватало
    одной буквы где угодно — и русский вопрос «Расскажи всё про акцию
    «Жетіқазына»» объявлялся казахским из-за названия акции. Помощник честно
    отвечал по-казахски человеку, который спросил по-русски (замер на проде
    22.08.2026, сессия 19). Название на другом языке — это ИМЯ, а не язык
    вопроса: одно такое слово среди трёх и более ничего не решает.

    Два казахских слова — уже язык. Одно, если слов всего одно-два, — тоже:
    «Сәлеметсіз бе» это приветствие, а не имя собственное.
    """
    lowered = str(text or '').lower()
    words = _ANY_WORD.findall(lowered)
    kazakh = [word for word in words if _KZ_LETTERS & set(word)]
    if len(kazakh) >= 2 or (kazakh and len(words) <= 2):
        return 'kk'
    # Маркеры ловят казахский БЕЗ специфических букв: «бонус бар ма» пишется
    # одними общими кириллическими буквами.
    if any(marker in lowered for marker in _KZ_MARKERS):
        return 'kk'
    return 'ru'


def detect_language(question, history=()):
    """Язык вопроса: 'kk' или 'ru'. Русский — язык по умолчанию.

    Определяется КОДОМ, а не оставляется на усмотрение модели. Причина замерена:
    на русском вопросе «Офис Астана» помощник отвечал по-казахски, и явное правило
    в промпте («если язык определить нельзя — отвечай по-русски») модель
    gemini-3.5-flash-lite игнорировала. Ровно как с уточняющим вопросом: то, что
    обязано соблюдаться, задаётся кодом, а промпт лишь дублирует.

    Реплика из одного-двух слов языка не показывает — она показывает НАЗВАНИЕ.
    «Жеті қазына» посреди русского разговора это уточнение про акцию, а не
    переход на казахский, и отвечать на него по-казахски неправильно. Поэтому у
    короткой реплики язык берётся из последней реплики человека. Это заметно в
    голосовом режиме, где короткие реплики — норма, а не исключение.
    """
    words = _ANY_WORD.findall(str(question or '').lower())
    if len(words) > 2 or not history:
        return _language_of(question)
    for turn in reversed(list(history)):
        if turn.get('role') == 'user' and str(turn.get('text') or '').strip():
            return _language_of(turn['text'])
    return _language_of(question)


_LANGUAGE_DIRECTIVE = {
    'ru': 'ОТВЕЧАЙ ТОЛЬКО ПО-РУССКИ. Язык вопроса — русский.',
    'kk': 'ЖАУАПТЫ ТЕК ҚАЗАҚ ТІЛІНДЕ БЕР. Вопрос задан по-казахски: весь ответ '
          'на казахском, содержание русских фрагментов переводи.',
}


def _squash(text):
    """Нормализация для дословной сверки: как в проверке цитат разбора чатов."""
    return ' '.join(str(text or '').split()).lower()


def meaningful_words(question):
    return _WORD.findall(str(question or '').lower())


# Сколько своих прошлых реплик подмешиваем в поисковый запрос и до какой длины
# вопроса это делаем. Оба числа — из разбора живого диалога, а не из головы.
FOLLOWUP_MAX_WORDS = 6
FOLLOWUP_TURNS = 2


def enrich_query(question, history):
    """Поисковый запрос для продолжения разговора.

    Короткая реплика сама по себе ищется плохо: «отправь ссылку для регистрации»
    не содержит темы вовсе. Раньше к ней подклеивалась ОДНА предыдущая реплика
    пользователя, и на живом диалоге этого не хватило:

        1. «что за акция Лимонопад»            — тема
        2. «по подробнее, как участвовать»      — подклеилась тема, всё хорошо
        3. «отправь ссылку для регистрации»     — подклеилась реплика №2, а в ней
                                                  темы уже нет

    То есть нить рвалась на втором шаге. Берём две последние реплики: тема
    доживает до третьего шага, а на четвёртом человек обычно уже называет её
    заново. Порог длины поднят с 4 слов до 6 — «отправь мне ссылку для
    регистрации» это пять слов и всё ещё отсылка к теме, а не самостоятельный
    вопрос.
    """
    words = meaningful_words(question)
    if len(words) > FOLLOWUP_MAX_WORDS:
        return question
    prior = [str(turn.get('text') or '').strip() for turn in (history or ())
             if turn.get('role') == 'user' and str(turn.get('text') or '').strip()]
    if not prior:
        return question
    return ' '.join(prior[-FOLLOWUP_TURNS:] + [question])


def should_clarify(question, chunks):
    """Нужен ли уточняющий вопрос — решает код, а не модель (см. шапку).

    Возвращает (нужно, причина). Причина уходит в журнал: без неё потом не
    понять, почему помощник переспросил.
    """
    words = meaningful_words(question)
    if len(words) > CLARIFY_MAX_WORDS:
        return False, None
    if not chunks:
        return False, None
    # ТОЧНОЕ СОВПАДЕНИЕ ТЕРМИНА снимает вопрос о двусмысленности. Замер на проде:
    # «что за акция Лимонопад» — три слова и невысокая близость, гейт переспросил,
    # хотя «Лимонопад» встречается ровно в одной статье и спрашивать тут не о чем.
    # Уточняющий вопрос на однозначный запрос раздражает сильнее, чем помогает:
    # человек уже назвал вещь своим именем.
    if any(chunk.get('strict_hit') for chunk in chunks):
        return False, None
    # Триграммный добор — тот же случай, только человек ошибся буквой:
    # «Лимонапад» вместо «Лимонопад». Ветка молчит, пока похожие куски не лежат
    # в одной статье, поэтому её попадание — это уже названное имя.
    if any(chunk.get('fuzzy_hit') for chunk in chunks):
        return False, None
    # …но strict_hit требует, чтобы в куске нашлись ВСЕ слова вопроса разом, и
    # этого хватает не всегда. «Что за акция 7 Казына?»: слово «казына» лежит
    # ровно в одном куске на всю вику, а слова «акция» в этом куске нет — он
    # табличная строка, — и строгое совпадение не срабатывает. Гейт переспрашивал
    # про вещь, названную своим именем (проверено на проде 22.08.2026).
    #
    # Поэтому второй признак того же самого: слово из вопроса встречается в
    # найденном тексте и принадлежит РОВНО ОДНОЙ статье. Тогда спрашивать не о
    # чем — переспросить хуже, чем ответить.
    if named_term(question, chunks):
        return False, None
    top = max((chunk.get('similarity') or 0) for chunk in chunks)
    if top >= CLARIFY_SIMILARITY:
        return False, None
    articles = {chunk['article_id'] for chunk in chunks}
    if len(articles) < CLARIFY_MIN_ARTICLES:
        return False, None
    return True, (f'коротко ({len(words)} сл.), лучшая близость {top:.3f} < '
                  f'{CLARIFY_SIMILARITY}, попадания в {len(articles)} разных статей')


# Отказную фразу ищем по смыслу, а не по букве. Модели её перефразируют: «в
# доступных вам статьях этого нет», «в доступных статьях нет информации», «в
# доступных мне материалах нет». Признак — предложение, где рядом стоят
# «доступн…» и «нет».
_REFUSAL_MARK = re.compile(r'[^.!?\n]*доступн[^.!?\n]*\bнет\b[^.!?\n]*[.!?]?', re.I)
_HAS_NUMBER = re.compile(r'\d')


def is_refusal(body):
    """Отказ ли это целиком — или ответ с честной оговоркой.

    Раньше признаком служило одно вхождение фразы «в доступных вам статьях этого
    нет» ГДЕ УГОДНО в тексте. Из-за этого полезный ответ вида «общей инструкции
    нет, но известно вот что: …» объявлялся отказом и ЛИШАЛСЯ ИСТОЧНИКОВ — то
    есть помощник наказывал сам себя ровно за то поведение, которого от него и
    хотят: сказать, чего нет, и дать то, что есть.

    Теперь смотрим, ЧТО ОСТАЁТСЯ после вычета отказной фразы, и содержательным
    считаем остаток с числом. Число — это срок, сумма, размер, дата: именно за
    ними приходят, и именно они не выдумываются (за этим следит
    ungrounded_numbers). Длина остатка признаком не годится: «Я могу подсказать
    только данные из базы знаний таксопарка про офисы и аренду» — длинно и ровно
    ни о чём.

    Ошибка в эту сторону дешёвая: текст человек видит целиком в любом случае,
    разница только в том, подписываем ли мы его статьями-источниками. Список
    статей под фразой «этого нет» читается как противоречие и подрывает доверие
    к самой фразе — поэтому у отказа источников быть не должно.
    """
    text = str(body or '').strip()
    if not text:
        return True
    rest = _REFUSAL_MARK.sub(' ', text)
    if rest.strip() == text:
        return False                      # отказной фразы нет вовсе — это ответ
    return not _HAS_NUMBER.search(rest)


def named_term(question, chunks):
    """Назвал ли человек вещь своим именем: слово из вопроса живёт в одной статье.

    Сверяем СВЁРНУТЫЕ формы: в вике «7 Қазына», а спрашивают «7 Казына» —
    казахские буквы должны перестать различаться, ровно как в поиске.

    Слова короче четырёх букв не берём: «все», «как», «что» встречаются где
    угодно, и одна статья у них случайна.
    """
    from ..text import fold_kazakh

    words = [fold_kazakh(word) for word in meaningful_words(question) if len(word) >= 4]
    if not words:
        return False
    folded = [(chunk.get('article_id'),
               fold_kazakh(f"{chunk.get('heading_path') or ''} {chunk.get('text') or ''}".lower()))
              for chunk in chunks]
    for word in words:
        owners = {article_id for article_id, text in folded if word in text}
        if len(owners) == 1:
            return True
    return False


def usable_chunks(chunks, floor=STRICT_FLOOR):
    """Куски, годные для ответа: близость выше порога либо найдены словом.

    Лексическое совпадение пропускаем без порога близости намеренно: точный
    термин или номер — сам себе доказательство, а близости у него может не быть
    вовсе (вектор промахивается на редких словах, лексика для этого и нужна).

    То же и для триграммной ветки (found_by = 2): близости у её кусков нет по
    построению, а нашлись они по имени, названному с ошибкой в одну букву —
    отбросить их порогом значило бы выбросить единственную находку.
    """
    out = []
    for chunk in chunks:
        similarity = chunk.get('similarity')
        by_word = bool({0, 2} & set(chunk.get('found_by') or []))
        if by_word or (similarity is not None and similarity >= floor):
            out.append(chunk)
    return out


def build_user_prompt(question, chunks):
    """Пронумерованные фрагменты + вопрос. Номера — ключ к сверке цитат.

    Метка фрагмента отделена от тела строкой «ТЕКСТ:» намеренно. В первом варианте
    название статьи и путь заголовков шли той же строкой, что текст, и модель на
    вопросе про самозанятого процитировала ЗАГОЛОВОК фрагмента: цитата не
    сверилась, и защита превратила верный ответ в отказ. Помощник, отрицающий то,
    что знает, дороже любой галлюцинации — он теряет доверие целиком.
    """
    blocks = []
    for number, chunk in enumerate(chunks, start=1):
        heading = chunk.get('heading_path') or ''
        title = chunk.get('title') or ''
        label = f'Статья «{title}»'
        if heading:
            label += f', раздел «{heading}»'
        blocks.append(f'[{number}] {label}\nТЕКСТ:\n{chunk["text"]}')
    context = '\n\n'.join(blocks) if blocks else '(подходящих фрагментов не найдено)'
    # Указание про язык идёт ПОСЛЕ вопроса: последняя строка запроса держится
    # лучше, чем правило в середине системного промпта, которое flash-lite
    # игнорировал на коротких вопросах.
    directive = _LANGUAGE_DIRECTIVE[detect_language(question)]
    return f'ФРАГМЕНТЫ СТАТЕЙ:\n{context}\n\nВОПРОС: {question}\n\n{directive}'


def split_sources(text):
    """Отделить блок ИСТОЧНИКИ от тела ответа и вынуть номера фрагментов."""
    body = str(text or '')
    match = _SOURCES_HEADER.search(body)
    if not match:
        return body.strip(), []
    tail = body[match.end():]
    numbers = []
    for raw in _FRAGMENT_REF.findall(tail):
        value = int(raw)
        if value not in numbers:
            numbers.append(value)
    return body[:match.start()].strip(), numbers


def _content_words(text):
    return set(_WORD.findall(str(text or '').lower()))


def pick_excerpt(chunk_text, answer, *, limit=240):
    """Выбрать из куска строку, которая ближе всего к ответу.

    Цитату подбирает СЕРВЕР, а не модель, и это переделка по результатам проверки
    на проде. Требование «процитируй дословно» оказалось ненадёжным механизмом:
    на вопросе «Офис Астана» поиск нашёл нужный кусок (близость 0,834, в тексте
    буквально «Город: Астана; Адрес: Проспект Сарыарка, 31»), но модель
    процитировала строку-метку «Статья «Адреса офисов», раздел «…»», сверка её в
    тексте не нашла, и защита выбросила ВЕРНЫЙ ответ. На соседнем вопросе та же
    модель цитировала корректно — то есть механизм срабатывал через раз.

    Извлечение на сервере снимает проблему целиком: цитата дословна по
    построению, потому что взята из текста куска программой.
    """
    lines = [line.strip() for line in str(chunk_text or '').splitlines()
             if line.strip()]
    if not lines:
        return ''
    target = _content_words(answer)
    # Числа весят больше слов: именно в них факт, за которым оператор и пришёл.
    # И они работают через язык — на казахский ответ пересечение русских слов
    # нулевое, и без этого признака подбор скатывался к первой строке куска
    # («Условия:» вместо строки со сроком).
    target_numbers = {_digits(token) for token in _NUMBER.findall(str(answer or ''))
                      if len(_digits(token)) >= 2}
    best, best_score = lines[0], -1.0
    for line in lines:
        line_numbers = {_digits(token) for token in _NUMBER.findall(line)}
        score = (2.0 * len(target_numbers & line_numbers)
                 + len(target & _content_words(line))
                 # Длина решает при равенстве: содержательная строка полезнее
                 # огрызка вроде «Условия:».
                 + min(len(line), 400) / 1000.0)
        if score > best_score:
            best, best_score = line, score
    return best if len(best) <= limit else best[:limit].rstrip() + '…'


def _digits(text):
    return re.sub(r'\D', '', str(text or ''))


_URL_IN_TEXT = re.compile(r'(?:https?://|mailto:)\S+', re.I)


def ungrounded_numbers(answer, chunks, question=''):
    """Числа из ответа, которых нет ни в одном переданном фрагменте.

    Это и есть машинная защита от выдумки — вместо сверки цитат, которая
    отбрасывала верные ответы. Проверяются числа от трёх знаков: именно в них
    живёт настоящий риск (сумма, срок, номер телефона, адрес), а короткие
    совпадают со нумерацией списков и давали бы ложные срабатывания.

    Адреса из проверки ВЫРЕЗАЮТСЯ. Замер на проде 12.08.2026: правка статьи «Все
    акции» получила предупреждение «числа, которых нет в документе: 992» — это
    был обломок идентификатора формы Google
    (…1FAIpQLSeO-wSvqAZ992Z_LWPbo…). Цифры внутри адреса не факт, на который
    кто-то опирается, а ложное предупреждение обесценивает все остальные: их
    перестают читать.
    """
    answer = _URL_IN_TEXT.sub(' ', str(answer or ''))
    haystack = _digits(' '.join(chunk.get('text') or '' for chunk in chunks))
    asked = _digits(question)
    bad = []
    for token in _NUMBER.findall(str(answer or '')):
        digits = _digits(token)
        if len(digits) < 3:
            continue
        if digits in haystack or digits in asked:
            continue
        if token.strip() not in bad:
            bad.append(token.strip())
    return bad


def _support_score(chunk, answer):
    """Насколько текст куска подтверждает ответ. Числа весят больше слов."""
    text = chunk.get('text') or ''
    answer_numbers = {_digits(token) for token in _NUMBER.findall(str(answer or ''))
                      if len(_digits(token)) >= 2}
    text_numbers = {_digits(token) for token in _NUMBER.findall(text)}
    return (5.0 * len(answer_numbers & text_numbers)
            + len(_content_words(answer) & _content_words(text)))


def build_sources(cited, chunks, answer):
    """Источники ответа: выбирает СЕРВЕР по доказательству, модель лишь подсказывает.

    Номера модели — подсказка, а не истина. Замер на корпусе: на вопросе о
    минимальном сроке аренды модель верно ответила «14 дней», но сослалась на
    кусок, в котором этого срока нет — под ответом оказывалась цитата про график
    6/1, ничего не подтверждающая. Показывать такую цитату хуже, чем не показывать:
    она выглядит доказательством, не будучи им.

    Поэтому куски ранжируются по совпадению с ответом (числа весят втрое), а
    указание модели даёт лишь надбавку. Выбранный кусок, которого модель не
    называла, помечается attributed — в интерфейсе это бейдж «сопоставлено».
    """
    ranked = sorted(
        ((_support_score(chunk, answer) + (2.0 if index in cited else 0.0), index)
         for index, chunk in enumerate(chunks, start=1)),
        key=lambda item: (-item[0], item[1]))
    used = [index for score, index in ranked if score >= SUPPORT_FLOOR][:3]
    if not used and cited:
        used = [number for number in cited if 1 <= number <= len(chunks)][:1]

    sources = []
    for number in used[:5]:
        chunk = chunks[number - 1]
        sources.append({
            'number': number,
            'quote': pick_excerpt(chunk.get('text'), answer),
            'ok': True,          # цитата извлечена программой — дословна всегда
            'attributed': number not in cited,
            'chunk_id': chunk['chunk_id'],
            'chunk_text_hash': chunk.get('text_hash'),
            'article_id': chunk['article_id'],
            'title': chunk.get('title'),
            'slug': chunk.get('slug'),
            'heading_path': chunk.get('heading_path'),
            'requires_ack': bool(chunk.get('requires_ack')),
        })
    return sources


NO_ANSWER_TEXT = ('В доступных вам статьях этого нет. Уточните у супервайзера '
                  'или в профильном отделе.')

_ACK_NOTE = ('Этот пункт входит в обязательное ознакомление по статье «{title}» — '
             'подтвердите ознакомление в самой статье.')


def ack_notice(sources):
    """Приписка про обязательное ознакомление — решение владельца 10.08.2026.

    Помощник не обходит процедуру, а загоняет в неё: отвечает по сути и отправляет
    подтвердить. Запись о подтверждении он не трогает, поэтому отчёт «кто
    ознакомлен» остаётся точным.
    """
    titles = []
    for source in sources:
        if source.get('ok') and source.get('requires_ack'):
            title = source.get('title') or ''
            if title and title not in titles:
                titles.append(title)
    return [_ACK_NOTE.format(title=title) for title in titles]


def compose(question, chunks, generate_fn, *, history=(), allow_clarify=True):
    """Собрать ответ. generate_fn(system, user, history=…) -> (текст, метаданные).

    history — предыдущие реплики диалога. Без них уточняющий вопрос был тупиком:
    помощник спрашивал «какой именно офис — Ipartner, Global, Taxi24?», а на ответ
    «Taxi24» отказывался, потому что реплика приходила в модель одна, без диалога.
    Поиск при этом находил нужный кусок первым же результатом — не помнила именно
    модель.

    allow_clarify=False запрещает переспрашивать. Ставится, когда предыдущая
    реплика помощника САМА была уточняющим вопросом: иначе на короткий ответ
    оператора помощник переспрашивал снова, и разговор ходил по кругу.

    Возвращает словарь с kind: 'answer' | 'no_answer' | 'clarify'.
    """
    usable = usable_chunks(chunks)

    if not usable:
        return {'kind': 'no_answer', 'text': NO_ANSWER_TEXT, 'sources': [],
                'notes': [], 'meta': {'reason': 'нет кусков выше порога'}}

    clarify, reason = (should_clarify(question, usable) if allow_clarify
                       else (False, None))
    if clarify:
        return {'kind': 'clarify',
                'text': ('Уточните вопрос: он допускает разные ответы. Про что '
                         'именно спрашиваете?'),
                'sources': [], 'notes': [],
                'meta': {'reason': reason}}

    # Язык считается ПО РАЗГОВОРУ, а не по одной реплике: короткое уточнение
    # своего языка не имеет (см. detect_language).
    wanted = detect_language(question, history)
    # Язык называем СРАЗУ, а не ждём промаха. Раньше первая попытка шла без
    # указания, и модель угадывала: на русском «Офис Астана» отвечала
    # по-казахски. Промах стоил целой лишней генерации — замер на проде
    # 22.08.2026: от 0,6 до 2,5 секунды, а в голосовом разговоре это половина
    # всей паузы перед ответом.
    prompt = build_user_prompt(question, usable) + '\n\n' + _LANGUAGE_DIRECTIVE[wanted]
    text, meta = generate_fn(SYSTEM_PROMPT, prompt, history=history)
    # Повтор остаётся страховкой: указание модель иногда всё равно игнорирует.
    # Если и он не помог — ответ отдаём как есть, потому что неудобно читать
    # хуже, чем неверно по сути, но не настолько, чтобы молчать.
    if wanted != detect_language(text):
        try:
            text, meta = generate_fn(
                SYSTEM_PROMPT,
                prompt + ' Предыдущая попытка была на другом языке — исправь.',
                history=history)
        except Exception:
            pass
    body, cited = split_sources(text)

    if is_refusal(body) or not body:
        # Отказ идёт без источников: список статей под фразой «этого нет» читается
        # как противоречие и подрывает доверие к самой фразе.
        return {'kind': 'no_answer', 'text': body or NO_ANSWER_TEXT,
                'sources': [], 'notes': [], 'meta': meta}

    invented = ungrounded_numbers(body, usable, question)
    if invented:
        # Единственное, из-за чего ответ теперь придерживается: числа, которых нет
        # ни в одном переданном фрагменте. Прежнее правило («нет подтверждённых
        # цитат») отбрасывало верные ответы — проверка на проде показала это на
        # вопросе «Офис Астана», где нужный кусок был найден и ответ существовал.
        return {'kind': 'no_answer', 'text': NO_ANSWER_TEXT, 'sources': [],
                'notes': [],
                'meta': dict(meta, rejected='числа не найдены в источниках',
                             ungrounded=invented, raw_preview=body[:300])}

    sources = build_sources(cited, usable, body)
    return {'kind': 'answer', 'text': body,
            'sources': sources, 'notes': ack_notice(sources), 'meta': meta}
