"""Раздел «Вики» — база знаний внутри OTP.

Логика раздела вынесена в отдельный пакет, а не в bot_schedule2.py/database.py,
по двум причинам:

1. bot_schedule2.py уже 48k строк и 362 плоских роута; +70 роутов вики сделали бы
   файл нечитаемым. Роуты вики собраны в Blueprint (wiki.routes) и подключаются
   к приложению одной строкой.
2. Тесты в проекте вынуждены парсить database.py через ast, потому что его импорт
   открывает пул к боевой БД. Модули wiki.access и wiki.text намеренно не импортируют
   ни database, ни flask — их можно импортировать в тестах напрямую.

Состав пакета:
    schema.py  — DDL всех таблиц wiki_*, идемпотентный
    access.py  — чистая логика прав: способности роли, сбор субъектов, слияние правил
    routes.py  — Blueprint с HTTP-эндпоинтами (подключается из bot_schedule2.py)
"""

__all__ = ['schema', 'access']
